---
const { title, url, image, alt, excerpt } = Astro.props;
---

<div class="flex flex-col gap-4">
  <img class="border aspect-square object-cover" src={image} alt={alt} />
  <div class="flex flex-col gap-1">
    <a class="hover:text-amber-500" href={url}>
      <h3>{title}</h3>
    </a>
    <p class="opacity-50">{excerpt}</p>
  </div>
</div>
